# ────────────────────────────────────────────────────────────────────────────
# YOS — Docker Compose
#
# Quick start:
#   1. Set environment variables below (or use a .env file in this directory)
#   2. docker compose up -d
#   3. docker compose logs -f
#
# Rebuilding after a source update:
#   docker compose build --pull && docker compose up -d
# ────────────────────────────────────────────────────────────────────────────

services:
  yos:
    build:
      context: .
    image: yos:local
    container_name: yos
    restart: unless-stopped

    # ── Authentication (required — choose ONE) ──────────────────────────────
    environment:
      # Option A: Claude Code OAuth token (Pro/Max subscription)
      CLAUDE_CODE_OAUTH_TOKEN: ${CLAUDE_CODE_OAUTH_TOKEN:-}
      # Option B: Anthropic API key (usage-based billing)
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}

      # ── Core config ───────────────────────────────────────────────────────
      TZ: ${TZ:-UTC}
      CLAUDE_BYPASS_PERMISSIONS: ${CLAUDE_BYPASS_PERMISSIONS:-true}

      # ── Web console ─────────────────────────────────────────────────────────
      # Bind to 0.0.0.0 inside container so ports are accessible from host
      WEB_CONSOLE_BIND: "0.0.0.0"
      YOS_WEB_PASSWORD: ${YOS_WEB_PASSWORD:-}

      # ── Channel tokens (optional) ─────────────────────────────────────────
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
      LARK_APP_ID: ${LARK_APP_ID:-}
      LARK_APP_SECRET: ${LARK_APP_SECRET:-}

    # ── Ports ────────────────────────────────────────────────────────────────
    ports:
      # Web console (default: 3456)
      - "${WEB_CONSOLE_PORT:-3456}:3456"
      # Caddy / reverse proxy (optional, set CADDY_ENABLED=true in .env)
      - "${HTTP_PORT:-8080}:8080"

    # ── Volumes ───────────────────────────────────────────────────────────────
    volumes:
      # Entire yos workspace — persists .env, memory, components, logs, etc.
      - yos-data:/home/yos/yos

      # Claude Code settings and auth tokens
      - claude-config:/home/yos/.claude

      # Optional: mount your own .env instead of relying on environment block
      # - ./yos.env:/home/yos/yos/.env:ro

    # ── Health ────────────────────────────────────────────────────────────────
    healthcheck:
      test: ["CMD", "pm2", "list"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 600s

    # ── Resources (optional, tune for your host) ──────────────────────────────
    # deploy:
    #   resources:
    #     limits:
    #       memory: 2g
    #     reservations:
    #       memory: 512m

# ── Named volumes ─────────────────────────────────────────────────────────────
volumes:
  yos-data:
    driver: local
  claude-config:
    driver: local
