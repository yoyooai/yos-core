# Feature: 自升级事务安全

## 背景

原自升级有两个结构问题：

1. `npm pack` 会触发全仓发布门禁，但客户下载的源码归档没有 `.git` 和开发依赖，因此升级必然在全局安装前失败。
2. 新核心安装后的步骤 5–12 失败时不回滚，可能留下新旧版本混合且服务停止的机器。

## 关键决策

### 1. 分开发布门禁和客户端候选包

- `npm run release:pack` 是正式制品入口：先运行完整 `verify`，然后打包。
- `prepublishOnly` 防止直接 `npm publish` 绕过门禁。
- 普通 `npm pack` 不再运行全仓门禁，仅用于客户端事务内生成已下载候选包。

否掉的方案：在无 `.git` 时让 `verify` 自动降级。这会让正式发布门禁在需要它时静默失效。

### 2. 在停服务前做真实可执行预检

- 检查临时目录、至少 512 MiB 可用空间和 npm 可用性。
- 把每个带生产依赖的 Core Skill 清单复制到隔离目录，真实执行 `npm install --omit=dev`。
- 预先生成新核心候选包。任一操作失败时，备份和停服务步骤都不会开始。

否掉的方案：硬编码检查 `make/gcc/python3`。原生依赖可能有适配当前平台的预编译包；真实安装预演比工具名清单更准确，也能提前发现 Registry、ABI 和安装脚本问题。

### 3. 核心包、Skill 和服务统一回滚

- 步骤 1 同时打包当前核心，备份目录权限为 `0700`，回滚状态为 `0600`。
- 步骤 4–12 失败时，依次恢复旧核心、Skill、Skill 依赖、PM2 配置和原先在线服务，最后验证服务确实 online。
- 自动回滚不完整时，返回 `machineState=recovery_required`、备份路径和 `yos upgrade --self --recover <backup>`。
- 步骤 13 只在服务已验证可用后提交基线；它失败时保持新版运行，明确告警并保留备份。

### 4. 成功清理与更新说明降级

- 备份清理移入核心 finalizer，所以人读与 `--json` 成功路径行为一致。
- 更新说明只从已下载包读取；缺失或不可读时用固定文案告警，不再访问第三方仓库。

### 5. 回滚结果必须由实际状态证明

- finalizer 交接状态升级为 schema v2；无版本号和 v1 按 legacy 处理，不假设旧核心包已备份；未来的更高版本明确拒绝。
- 回滚结果区分 `attempted` 和 `performed`：执行过回滚不等于已完成回滚。
- 核心版本必须与升级前版本一致；Core Skills 必须与事务备份的目录、文件内容和符号链接一致；服务仍必须 online。
- 任一验证失败都进入 `recovery_required`，回复中显式列出实际/期望核心版本、Skill 恢复状态、备份路径和可执行的手动恢复命令。

否掉的方案：只根据“回滚步骤都返回成功”判定整体成功。这会在旧核心未备份或恢复内容不一致时产生假全清报告。

### 6. 恢复提示必须可直接执行

- 自动回滚不完整时，人读输出必须同时说明回滚已尝试但未完成、当前核心版本、Core Skills 版本、是否处于混装状态、备份目录和恢复命令。
- 未恢复为 online 的服务逐项列出，避免用户只看到笼统的 `recovery_required`。
- 同步设置钩子遇到 `EISDIR` 或不支持的文件类型时，只输出固定可操作文案；不透传绝对路径、Node 调用栈或原始系统错误。

否掉的方案：只在 JSON 结构里保留恢复细节。自动化能读到不等于现场操作者能恢复机器，人读和机器读两条路径必须同时诚实。

## 升级路径网络依赖

| 阶段 | 端点 / 命令 | 必要性 | 失败行为 |
|---|---|---|---|
| 检查版本 | GitHub tags API；分支模式读取发布源 `package.json` | 必需 | 升级不开始，服务不停 |
| 下载候选 | 配置的 YOS GitHub 发布源 archive；私有仓失败时可用 GitHub API | 必需 | 升级不开始，服务不停 |
| 预装及恢复 Skill 依赖 | npm Registry | 必需 | 预检阶段拒绝；若回滚恢复依赖失败则显式标记恢复不完整 |
| 更新说明 | 无额外请求，仅读已下载的 `CHANGELOG.md` | 可选 | 固定告警后继续 |

## 验证记录

- 无 `.git` 且无 `node_modules` 的源码副本：`npm pack --ignore-scripts` 成功。
- 真实预检：5 个带生产依赖的 Core Skill 安装预演成功，候选 tgz 生成成功。
- 定向测试：发布门禁、预检、回滚、步骤 13 告警、恢复入口、JSON 备份清理、可选更新说明均通过。
- 全量门禁：Jest `177/177`，Node `1055/1055`，6 个依赖根 0 漏洞，包 374 项，内容摘要 `7bd9ca71c21d079f7853e00404576b16c5d0ae1e2562f9f7a75fdcef8221233d`。
- `npm run release:pack` 真实生成 697844 字节制品，SHA-256 `9a5e797bfeee0f85bb68a0bd8bcec78ebe92ce18ddc57c4bc5a5b3e50730cf3e`，与门禁内双构建一致。
- 上述数字来自 macOS Node 24.10.0 / npm 11.6.0 开发自测；锁定 Linux 环境的双构建与真实隔离升级/故障回滚仍由独立验收人执行。
- 回滚真实性收口后定向与全量回归：Jest `177/177`，Node `1063/1063`，6 个依赖根 0 漏洞，包 375 项，内容摘要 `2a2b45273f1c6df1e4584f8655071a860922bc1e8b90aa1739c5f20fb9ea91df`。
- macOS Node 24.10.0 / npm 11.6.0 双构建 SHA-256 均为 `108b767fb9ece12367abc06018efbb09824ddc84fc88e33503997c38b70bd680`，`npm run verify` PASS，工作区干净。
- A1/A2/A3/A6 真实升级故障场景需使用独立 Linux Docker 台架 `workspace/yos-upgrade-probe-20260803/` 复验；该台架不在开发机，本记录不把自动化单测替代为真机结论。
- 2026-08-04 收官回归：Jest `194/194`，Node `1064/1064`，6 个依赖根 0 漏洞，包 383 项，内容摘要 `86f8622bc49f9370752629811a45eeb41117552e8f7a7af5492e48f7f50fca77`，macOS 双构建 SHA-256 `08b09cd805204ed3d8413f4df1558b9a4f04ebca31b07203c9c27aa154938b90`，`npm run verify` PASS。
- 旧核心首次升级兼容回归：error 字段同时携带失败原因和 RECOVERY 恢复说明，覆盖“已尝试但未完成”、实际/期望核心版本、Core Skills 状态、混装状态、失败服务、备份目录和恢复命令；当前 formatter 检测同一说明后不重复输出。
- 本轮开发侧整仓门禁：Jest 195/195、Node 1065/1065、6 个依赖根 0 漏洞、包 383 项，内容摘要 f405d10a11a38c5b8d4266062644cbb8d21becacae4996d303003137f5f4a2aa，macOS 双构建 SHA-256 e562ba1a29ca5586e91c83fa5492d1c3c1a24620786a540cb955d8a7493848e7，npm run verify PASS。

## 影响范围

仅影响 YOS Core 正式打包和 `yos upgrade --self`。不改组件升级、运行时、消息、记忆、渠道和数据库契约。
