# Shared Preferences

Preferences that apply across all users. Per-user preferences go in users/<id>/profile.md.
See yos-memory skill `references/preferences-format.md` for entry format.

(No shared preferences recorded yet.)
