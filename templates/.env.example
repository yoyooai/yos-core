# YOS Configuration
# Copy this file to ~/yos/.env and fill in your values

# ============================================
# CORE SETTINGS (Required)
# ============================================

# Timezone for scheduler and memory scripts (session rotation, daily snapshots)
# Use an IANA zone name such as UTC, America/New_York, or Asia/Shanghai
TZ=UTC

# ============================================
# OPTIONAL SETTINGS
# ============================================

# Run Claude with --dangerously-skip-permissions (for autonomous agent mode)
# Set to false if you want Claude to ask for permission before running commands
CLAUDE_BYPASS_PERMISSIONS=true

# Authentication (choose one — do not set both)
# Option 1: Setup token from "claude setup-token" (uses Pro/Max subscription)
# CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-xxx
# Option 2: Anthropic API key (usage-based billing)
# ANTHROPIC_API_KEY=sk-ant-xxx
# Optional: custom Anthropic-compatible base URL for Claude Code
# ANTHROPIC_BASE_URL=https://api.example.com

# Codex (OpenAI) runtime authentication
# OPENAI_API_KEY=sk-xxx
# Optional: custom OpenAI-compatible base URL for Codex
# OPENAI_BASE_URL=https://api.example.com/v1
# Proxy (if needed for external API access)
# HTTP_PROXY=http://proxy:port
# HTTPS_PROXY=http://proxy:port

# ============================================
# CHANNEL CONFIGS (Optional - add as needed)
# ============================================

# Telegram Bot (if using a Telegram channel component)
# TELEGRAM_BOT_TOKEN=your_bot_token

# Lark Bot (if using a Lark channel component)
# LARK_APP_ID=your_app_id
# LARK_APP_SECRET=your_app_secret

# Web Console
# YOS_WEB_PASSWORD=your_password_here

# Operations alert target for inbound messages that exhaust delivery retries.
# Both values are required; alerts contain metadata only, never user content.
YOS_ADMIN_CHANNEL=
YOS_ADMIN_ENDPOINT=
